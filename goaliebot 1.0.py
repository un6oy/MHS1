# #!/usr/bin/env python3
import ev3dev.ev3 as ev3
import time
import sys
import "motortesting.py" as motors

#while True:
    
    #if IR.value(0) < 4:
        #move left 1cm
        #check colour
    #elif IR.value(0) == 5:
        #move forward 1cm
        #move backwards 1cm
        #check colour
    #elif Ir.value(0) > 6:
        #move right 1cm
        #check colour

motors.right()